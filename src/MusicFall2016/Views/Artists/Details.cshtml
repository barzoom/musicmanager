@model MusicFall2016.Models.Artist
@{
    ViewData["Title"] = "Artists";
}
    <h2>Artist Details</h2>
    <div class="row">
        <label class="col-md-2">Name:</label>
        <div class="col-md-10">@Model.Name</div>
    </div>
    <div class="row">
        <label class="col-md-2">Bio:</label>
        <div class="col-md-10">@Model.Bio</div>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th>Albums</th>
            </tr>
        </thead>
        <!--// foreach (var album in Model)
        {
            <tr>
                <td>album.Title</td>
                <td></td>
                <td></td>
                <td>album.Price</td>
            </tr> 
        }
            -->
    </table>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <a asp-action="Edit" asp-route-id="@Model.ArtistID" class="btn btn-sm btn-warning" style="background-color:black">Edit</a>
            <a class="btn btn-sm btn-warning" href="/Artists">Back to Artists</a>
        </div>
    </div>